import Layout from "../communs/layout";

const Home = () => {
    return (
        <Layout>
            <h1 className="text-4xl font-bold text-center mt-10">Home</h1>
        </Layout>
    );
};

export default Home;
